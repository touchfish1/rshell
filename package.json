{
  "name": "rshell-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:tauri": "npx @tauri-apps/cli@2 dev",
    "build:tauri": "npx @tauri-apps/cli@2 build",
    "dev:ui": "npm --prefix ui run dev",
    "build:ui": "npm --prefix ui run build"
  }
}
